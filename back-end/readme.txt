
### Build and launch the back end

1. Run `npm install` to install all dependencies listed in the `package.json` file.
2. Run `(sudo) nodemon server.js` to launch the back-end server
3. Create an .env file. (Please ask us for the content of the .env).